kind = "web"
previewPath = "/"
title = "Movie Club"
version = "1.0.0"
id = "artifacts/movie-club"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25158

[services.development]
run = "pnpm --filter @workspace/movie-club run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/movie-club", "run", "build" ]
publicDir = "artifacts/movie-club/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25158"
BASE_PATH = "/"
